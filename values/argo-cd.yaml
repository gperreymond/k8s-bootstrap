crds:
  install: true
  
global:
  domain: argo-cd.docker.localhost
  image:
    tag: v2.11.0-rc1
  logging:
    format: json

configs:
  params:
    "server.insecure": true
    "server.disable.auth": true